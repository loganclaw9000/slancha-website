# Example environment file for Playwright tests
# Copy to .env.local and configure for your environment

# Base URL for the application (where the dev server runs)
VITE_BASE_URL=http://localhost:4173
